// Can infer date type for today variable, hover over today to check
const today = new Date();
today.getMonth();

// Can infer type of age variable based on the value, hover over person to check
const person = {
  age: 20,
};

// Insance of colour, red, can be tracked back to the color it is created from
class Color {}
const red = new Color();
